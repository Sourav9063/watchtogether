import MusicApp from "@/components/music/MusicApp";

export const metadata = {
  title: "Music | Sync Play",
  description:
    "Browse music, build playlists, and listen with synchronized lyrics in Sync Play.",
};

export default function MusicPage() {
  return <MusicApp />;
}
