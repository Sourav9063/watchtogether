import { TEXTS } from "./texts";

export const metadata = {
  title: TEXTS.metadata.title,
  description: TEXTS.metadata.description,
};

export default function PrizeBondLayout({ children }) {
  return children;
}
